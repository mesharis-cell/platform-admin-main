export interface CompanyDomain {
            id: string;
            platform_id: string;
            company_id: string;
            hostname: string;
            type: string;
            is_verified: boolean;
            is_active: boolean;
            created_at: string;
            updated_at: string;
        }